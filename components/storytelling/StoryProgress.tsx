import type {StoryAct} from "@/types/story"; export function StoryProgress({acts,active,onSelect}:{acts:StoryAct[];active:number;onSelect:(i:number)=>void}){return <aside className="story-progress" aria-label="Story navigation"><div className="progress-line"><span style={{height:`${((active+1)/acts.length)*100}%`}}/></div>{acts.map((a,i)=><button key={a.id} className={i===active?"active":""} onClick={()=>onSelect(i)} aria-label={`Go to ${a.label}`} aria-current={i===active?"step":undefined}>{a.number}</button>)}</aside>}